import { sortScores } from "../app/shared/score/scoreUtils";
import { Score } from "~/app/shared/score/score";

describe("Score Sorting", () => {
  let scores: Score[];

  beforeEach(() => {
    scores = [

    ]
  })

  it("Can sort by date ascending", () => {

  })
})